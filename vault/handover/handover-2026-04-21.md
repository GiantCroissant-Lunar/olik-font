---
title: Session handover — 2026-04-21
created: 2026-04-21
tags: [type/handover]
source: self
status: active
---

# Session handover — 2026-04-21 (AM)

> Read this first when resuming. Short enough to scan, long enough that a fresh session can pick up without re-deriving decisions.

## What this project is

**olik-font** — research + tooling around Chinese font generation via a component-based glyph scene graph. Current active design thread: stylable prototypes composed by a constraint-based scene graph, with ComfyUI reserved for styling (not reconstruction). Distilled spec lives in [[glyph-scene-graph-spec]], raw AI thread in `vault/references/discussion/`.

## Project layout

```
olik-font/
├── CLAUDE.md                 # pointer → AGENTS.md (gitignored) or .agent/skills/INDEX.md
├── AGENTS.md                 # local per-dev agent overlay (gitignored, template written)
├── .agent/skills/            # committed shared agent skills — 6 skills, numbered categories
│   ├── INDEX.md
│   ├── 00-meta/
│   │   ├── vault-obsidian/        (+ references/ companion files)
│   │   └── archon-workflows/
│   └── 04-tooling/
│       ├── notebooklm-cli/
│       ├── ref-projects/
│       ├── youtube-gemma/
│       └── coding-providers/
├── vault/                    # Obsidian knowledge vault
│   ├── references/discussion/   # raw AI thread: Canjile → ComfyUI → scene graph → constraints
│   ├── specs/                   # distilled — glyph-scene-graph-spec + Excalidraw pipeline diagram
│   └── handover/                # ← this doc
├── infra/
│   └── .env                 # provider creds + endpoints (gitignored)
├── ref-projects/            # read-only upstream clones (gitignored) — excalidraw/, references/
└── project/                 # empty, reserved for project code
```

## Active skills (see `.agent/skills/INDEX.md`)

| Skill | Category | Role |
|---|---|---|
| `vault-obsidian` | 00-meta | Vault conventions: frontmatter, wiki-links, MOCs, diagrams, intake workflow |
| `archon-workflows` | 00-meta | Deterministic worktree-isolated AI workflows. Codex SDK only, PostgreSQL backing. |
| `notebooklm-cli` | 04-tooling | `nlm` CLI for NotebookLM — install, frequent re-login, free-tier-aware |
| `ref-projects` | 04-tooling | Read-only upstream clones under `ref-projects/`, hard no-edit rule |
| `youtube-gemma` | 04-tooling | yt-dlp → whisper → Gemma 4 (`:31b`, cloud-routed) → vault note |
| `coding-providers` | 04-tooling | CLI↔provider map. One CLI, one job. |

## Project conventions (authoritative — see `.agent/skills/INDEX.md` § Project-level conventions)

- **Archon uses Codex SDK only** for AI nodes. No Claude Code SDK. Non-Codex assistants (Pi / Kimi / Copilot) run via `bash:` nodes.
- **Archon backing store is PostgreSQL**, not SQLite.
- **One CLI, one job.**
  - Pi = ZAI / GLM only (even though upstream Pi supports many providers)
  - Codex = OpenAI + Archon's SDK backend
  - Kimi = Moonshot
  - Copilot = optional GitHub-native shell helpers
  - Ollama = Gemma 4 (`:31b`, cloud-routed)
- **Gemma 4 is cloud-only for 31b.** 31b is too large to run locally; `ollama signin` routes to cloud. Smaller tags could run locally but are too weak. **Never** `ollama pull gemma4:31b`.
- **OAuth-only for Codex and Copilot.** Both accept env-var auth upstream; we forbid it here — keeps plaintext keys out of `.env` and rotation tied to the subscription.
- **No LiteLLM / no Mastra.** Pi already unifies ZAI; Archon handles workflows. Adding a router is pure overhead on current scope.
- **`ref-projects/` is read-only.** Gitignored. Never edit, stage, or commit anything under it.
- **Secrets in `infra/.env`.** Only Pi (ZAI), Ollama, and the optional Kimi SDK fallback need env entries.

## CLI + endpoint state (verified 2026-04-21)

| CLI | Version | Auth | Status |
|---|---|---|---|
| Codex | 0.120.0 | ChatGPT OAuth | ✅ end-to-end smoke test passed (model `gpt-5.4`) |
| Copilot | 1.0.32 | GitHub OAuth | ✅ end-to-end smoke test passed |
| Kimi | 1.33.0 | OAuth | ✅ end-to-end smoke test passed |
| Pi | 0.65.2 | `ZAI_API_KEY` env | ⚠️ auth works, but ZAI coding plan rate-limited (5-hour rolling window; resets ~10:28:59 today) |
| Ollama | — | daemon running; `ollama signin` **not yet run** | ✅ bridge works end-to-end with locally-pulled model (`gemma3:4b`). Cloud `gemma4:31b` path blocked on signin. |

ZAI models confirmed available via `/models`: `glm-4.5`, `glm-4.5-air`, `glm-4.6`, `glm-4.7`, `glm-5`, `glm-5-turbo`, `glm-5.1`. Default: `glm-5.1`.

## `infra/.env` shape (gitignored — real values live on disk)

```
ZAI_API_KEY=…           ZAI_BASE_URL=https://api.z.ai/api/coding/paas/v4/   ZAI_DEFAULT_MODEL=glm-5.1
OLLAMA_API_KEY=…        OLLAMA_BASE_URL=http://localhost:11434/v1            OLLAMA_DEFAULT_MODEL=gemma4:31b
KIMI_API_KEY=… (SDK-only, optional — CLI is OAuth)   KIMI_BASE_URL=https://api.kimi.com/coding/v1
ARCHON_DATABASE_URL=postgres://archon:CHANGE_ME@localhost:5432/archon
CODEX_BIN_PATH=         (blank — fill with `which codex` output, currently /opt/homebrew/bin/codex)
```

No `OPENAI_API_KEY`, no `COPILOT_GITHUB_TOKEN` — both explicitly forbidden here.

## Open items / pick up here

1. **`ollama signin`** — interactive, one-time. Unlocks cloud routing for `gemma4:31b`. Not run yet.
2. **Fill `CODEX_BIN_PATH`** in `infra/.env` (current `which codex` = `/opt/homebrew/bin/codex`).
3. **Install Archon** — `brew install coleam00/archon/archon` or clone `github.com/coleam00/Archon` + `bun install`. Then configure PostgreSQL backing + Codex-only assistants in `~/.archon/config.yaml`. Not started yet.
4. **Populate `AGENTS.md`** — it's a template right now; personal preferences, machine paths, in-progress notes go there.
5. **Retry ZAI/Pi** once the 5-hour rate window resets (~10:28:59 today) to close the last verification gap.
6. **Glyph scene graph spec** is a draft — eventual next design step is formalizing the constraint vocabulary and picking one example character (漢 or 語) to walk end-to-end through the pipeline.

## How to enter a new session

1. Read `CLAUDE.md` → then `AGENTS.md` (local) → then `.agent/skills/INDEX.md`.
2. Source env for any provider work: `set -a; source infra/.env; set +a`
3. For glyph-scene-graph work, start from [[glyph-scene-graph-spec]] not the raw discussion files.
4. Any new skill goes under `.agent/skills/<category>/<name>/SKILL.md` with category/layer/related_skills frontmatter (see existing ones as templates).
5. Any new vault note follows the `@vault-obsidian` conventions (frontmatter + wiki-links + MOC updates).

## Reference trail

- Design thread MOC: [[vault/references/discussion/index]]
- Distilled spec: [[glyph-scene-graph-spec]]
- Pipeline diagram: [[glyph-scene-graph-pipeline.excalidraw]]
- Skills index: `.agent/skills/INDEX.md`
