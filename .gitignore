.vscode/

# Root-only — we don't want this to match .agent/skills/04-tooling/ref-projects/
/ref-projects/

.env

# macOS noise
.DS_Store

# Per-developer agent overlay — each dev maintains their own, treat like .env
AGENTS.md
